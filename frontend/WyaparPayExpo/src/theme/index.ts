/**
 * WyaparPay Design System
 * Central export for all theme tokens
 */

import colors, { semanticColors, withOpacity } from './colors';
import typography, { textStyles } from './typography';
import spacing, { borderRadius, shadows } from './spacing';

export const theme = {
  colors,
  semanticColors,
  typography,
  textStyles,
  spacing,
  borderRadius,
  shadows,
  withOpacity,
};

export {
  colors,
  semanticColors,
  withOpacity,
  typography,
  textStyles,
  spacing,
  borderRadius,
  shadows,
};

export default theme;
